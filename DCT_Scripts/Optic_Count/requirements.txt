# Runtime dependencies — same file is used by the Dockerfile and local venv.
# Use Python 3.11 locally to match the container image.
# Install: pip install -r requirements.txt
#
# Pinned for reproducible behavior (numerics, APIs) across laptop and Docker.

flask==3.1.3
gunicorn==25.3.0
psycopg2-binary==2.9.12
numpy==2.4.4
pandas==2.3.3
openpyxl==3.1.5
python-calamine==0.6.2
certifi==2026.4.22
pypdf==6.10.2
tenacity==9.1.4
google-auth==2.50.0
google-api-python-client==2.195.0
APScheduler==3.11.2
